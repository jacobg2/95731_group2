# Provenance Log

Record of which files/functions were agent-generated versus human-written.
Update this file when adding or substantially rewriting code.

## Agent-generated (Claude Code, 2026-07-04)

Everything below was generated by Claude Code from the project brief, then
verified by running the test suite, a live server smoke test, ruff, and a
Docker image build.

| Path | Contents |
|---|---|
| `config/` (settings, urls, wsgi, asgi) | Django project config; all env-driven settings |
| `chat/views.py` | `index()` form view, `api_ask()` JSON endpoint |
| `chat/services.py` | `ask_question()` OpenAI Responses API wrapper, `AssistantError` |
| `chat/forms.py` | `QuestionForm` |
| `chat/urls.py`, `chat/apps.py`, `chat/models.py`, `chat/admin.py` | App wiring (models/admin are placeholders) |
| `chat/templates/chat/index.html` | Chat form page |
| `chat/tests.py` | 8 view/API tests (OpenAI mocked) |
| `chat/management/commands/upload_reference.py` | Vector store creation + file upload command |
| `manage.py` | Standard Django entry point |
| `Dockerfile`, `docker-compose.yml`, `.dockerignore` | Container setup shared by devcontainer and deployment |
| `.devcontainer/devcontainer.json` | VS Code devcontainer (compose-based, extensions, postCreateCommand) |
| `requirements.txt`, `requirements-dev.txt`, `pyproject.toml` | Dependencies and ruff/mypy config |
| `.env.example`, `.gitignore` | Config template and ignore rules |
| `reference/knowledge.txt` | Placeholder reference document |
| `README.md` | Rewritten from the original stub |
| `architecture.md`, `CLAUDE.md`, `provenance.md` | Documentation |

## Human-written

| Path | Contents |
|---|---|
| `README.md` (original stub, pre-2026-07-04) | Initial repo README, since replaced — see git history (`ecf7f92`, `b3298a4`) |
| `.env` | Local secrets file — values filled in by humans; not in git |

## Review notes

_Placeholder — add findings from human code review here (reviewer, date,
file, note)._

| Date | Reviewer | File(s) | Note |
|---|---|---|---|
| | | | |
